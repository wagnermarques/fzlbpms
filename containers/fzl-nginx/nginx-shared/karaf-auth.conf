# Placeholder — overwritten at container start by
# docker-entrypoint.d/11-karaf-basic-auth.sh with the Authorization header
# nginx presents to Karaf. Kept in the image so `include` always resolves.
