---
services:

  fzl-nginx:
    build:
      context: ./containers/fzl-nginx
      dockerfile: Dockerfile
    container_name: fzl-nginx
    ports:
      - "80:80"
      - "8899:8899" #01_fzlbpms.conf -> fzlbpms internal apps
      - "8900:8900" # 
    volumes:
      - ./src-projects/var_www/html:/var/www/html:rw #na conf do nginx: root /var/www/html;
    networks:
      - fzl-network

  fzl-php8.1-fpm:
    build:
      context: ./containers/fzl-php8.1-fpm/
      dockerfile: Dockerfile
    container_name: fzl-php8.1-fpm
    volumes:
      - ./src-projects/var_www/html:/var/www/html
    networks:
      - fzl-network

  fzl-php8.3-fpm:
    build:
      context: ./containers/fzl-php8.3-fpm/
      dockerfile: Dockerfile
    container_name: fzl-php8.3-fpm
    volumes:
      - ./src-projects/var_www/html:/var/www/html
      - ./fzl-php8.3-fpm/moodledata:/var/www/moodledata
    networks:
      - fzl-network
    environment:
       - MOODLE_XMLRPC_ENABLED=0
       - MOODLE_MNET_DISPATCHER_MODE=off
      
  fzl-mysql:
    image: mysql:8.4.6
    container_name: fzl-mysql
    ports:
      - "3316:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: fzl_db
      MYSQL_USER: fzl_user
      MYSQL_PASSWORD: 1234
    volumes:
      - ./src-projects:/sql-scripts
      - ./containers/fzl-mysql/mysql-data:/var/lib/mysql
      #- edutec2rodrr:/var/lib/mysql
      #- /home/wgn/fzlbpms/containers/fzl-mysql/mysql-data:/var/lib/mysql
      #- fzl-mysql-data:/var/lib/mysql
      
    networks:
      - fzl-network

  fzl-postgresql:
    build:
      context: ./containers/fzl-postgresql
    container_name: fzl-postgresql
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: fzldb
      POSTGRES_INITDB_ARGS: "--locale=pt_BR.UTF-8" # Para garantir o locale na criacao do DB
    volumes:
      - ./src-projects:/sql-scripts
      - ./containers/fzl-postgresql/init-scripts:/docker-entrypoint-initdb.d
      - ./containers/fzl-postgresql/postgresql-data:/var/lib/postgresql/data
    networks:
      - fzl-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      
  fzl-karaf-camel-integration:
    build:
      context: ./containers/fzl-karaf-camel-integration
      dockerfile: Dockerfile
    container_name: fzl-karaf-camel-integration
    volumes:
      - ./src-projects/karaf_bundles:/opt/karaf/deploy_bundles
      - ./src-projects/karaf_deploy_dir:/opt/karaf/deploy
      - ./src-projects/karaf_input_dir:/opt/karaf/input_data
      - ./src-projects/karaf_output_dir:/opt/karaf/output_data
      - ./src-projects/karaf_project_sources_dir:/opt/karaf/sources:rw
      - ./containers/fzl-karaf-camel-integration/karaf-data:/opt/karaf/data
    networks:
      - fzl-network
    restart: unless-stopped

  fzl-angular-dev:
    image: fzl-angular-dev:0.0.1
    build:
      context: ./containers/fzl-angular-dev
      dockerfile: Dockerfile
      target: development
    container_name: fzl-angular-dev
    ports:
      - "4200:4200"
    volumes:
      - ./containers/fzl-angular-dev/src-projects/:/app
    networks:
      - fzl-network
          

  # a porta 8081 do nexus ta conflitando com a 8081 do fakermail
  # corrir qdo for usar o nexus
  fzl-nexus:
    build:
      context: ./containers/fzl-nexus
    container_name: fzl-nexus
    restart: always
    ports:
      - "8088:8081"
      - "8082:8082"
    volumes:
      - ./containers/fzl-nexus/nexus-data:/nexus-data
    environment:
      # Aumenta a memoria RAM disponivel para o Nexus (ajuste conforme necessario)
      # Nexus exige bastante RAM, o padrao pode ser insuficiente.
      # Exemplo: 2GB de heap
      MAX_HEAP_SIZE: "2048m"
      MIN_HEAP_SIZE: "2048m"
      # Ou usar JAVA_OPTS para mais controle:
      # JAVA_OPTS: "-Xms2g -Xmx2g -Djava.net.preferIPv4Stack=true"
    networks:
      - fzl-network
    healthcheck:  # Adiciona um healthcheck basico para verificar se o Nexus esta UP
      test: ["CMD-SHELL", "curl -f http://localhost:8088/nexus/repository/maven-public/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s # Da mais tempo para o Nexus iniciar
      
  fzl-phpmgyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: fzl-phpmyadmin
    ports:
      - '8889:80'
    environment:
      - PMA_ARBITRARY=1
      - PMA_HOST=fzl-mysql
    depends_on:
      - fzl-mysql
    networks:
      - fzl-network

  fzl-elasticsearch:
    build:
      context: ./containers/fzl-elasticsearch
      dockerfile: Dockerfile-Elasticsearch
    container_name: fzl-elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=true  # Ative seguranÃ§a para produÃ§Ã£o
    ports:
      - "9200:9200"
    volumes:
      - ./containers/fzl-elasticsearch/es-data:/usr/share/elasticsearch/data
    networks:
      - fzl-network
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s      

  fzl-kibana:
    build:
      context: ./containers/fzl-elasticsearch
      dockerfile: Dockerfile-Kibana
    container_name: fzl-kibana
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    ports:
      - "5601:5601"
    depends_on:
      - fzl-elasticsearch
    networks:
      - fzl-network      
      
  # aguardando pra qdo for utilizar
  #fzl-overleaf:
  #  build:
  #    context: ./containers/fzl-overleaf
  #    dockerfile: Dockerfile
  #  container_name: fzl-overleaf
  #  restart: unless-stopped
  #  ports:
  #    - "8090:80" # Access Overleaf at http://localhost:8090
  #  volumes:
  #    # This volume is CRITICAL to persist your LaTeX projects
  #    - ./containers/fzl-overleaf/overleaf-data:/var/lib/sharelatex
  #  networks:
  #    - fzl-network      


  fzl-mongodb: #used by overleaf container
    image: mongo:4.4
    container_name: fzl-mongodb
    restart: unless-stopped
    volumes:
      # Critical to persist MongoDB data
      - ./containers/fzl-mongodb/mongodb-data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: overleaf
      MONGO_INITDB_ROOT_PASSWORD: overleaf_pass
      MONGO_INITDB_DATABASE: overleaf
    networks:
      - fzl-network
    healthcheck:
      test: ["CMD-SHELL", "mongo --eval 'db.adminCommand(\"ping\")'"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  fzl-redis: #used by overleaf container
    image: redis:7-alpine
    container_name: fzl-redis
    restart: unless-stopped
    volumes:
      - ./containers/fzl-redis/redis-data:/data
    command: redis-server --appendonly yes  # Enable persistence
    networks:
      - fzl-network
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  fzl-fakemail:
    container_name: fzl-fakemail
    image: gessnerfl/fake-smtp-server:latest
    restart: always
    environment:
      - MH_UI_AUTH_FILE=/maildata/auth.txt
    #ports:
    #  - "5010:8080"
    #  - "8035:8025"
    network_mode: host #(entender melhor depois isso)
    #networks:
    #  - fzl-network

    
    # portainer to manage docker containers
  fzl-portainer:
    image: portainer/portainer-ce:latest
    container_name: fzl-portainer
    restart: always
    ports:
      - "9000:9000" # Porta web para acesso ao Portainer
      - "9043:9043"
    volumes:
      # Monta o socket Docker do Host para que o Portainer possa gerenciar o Docker
      - /var/run/docker.sock:/var/run/docker.sock
      # Volume nomeado para persistir as configurações do Portainer
      - ./containers/fzl-portainer/portainer-data:/data
    #networks:
    #  # Conecta à rede fzl-network para ter visibilidade dos outros contêineres
    #  - fzl-network
    

  fzl-rust-restservices:
    build:
      context: ./containers/fzl-rust-restservices
      dockerfile: Dockerfile
    container_name: fzl-rust-restservices
    ports:
      - "8089:8080"
    networks:
      - fzl-network

networks:
  fzl-network:
    driver: bridge
